#include "main.h"
#include <string.h>

/**
 * _strlen - parameter for length of a string
 *@s - parameter a string pointer
 * return: return length of stringg
 */

int _strlen(char *s)
{
		return (strlen(s));
}
