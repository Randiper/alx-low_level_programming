#include "main.h"
#include <stdio.h>

/**
 *print_rev - prints a string in reverse
 *@s: a string to be printed in reverse
 */

void print_rev(char *s)
{
	int n;

	while (s[n] != '\0'; n==)
	{
		_putchar(s[n]);
	}
	_putchar('\n');
}
